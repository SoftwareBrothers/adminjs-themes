import type { ThemeConfig } from 'adminjs';

export const theme: Partial<ThemeConfig['data']> = {
  colors: {
    bg: '#FFFFFF',
    container: '#FFFFFF',
    defaultText: '#1C1C38',
    border: '#F9F9F9',
    sidebar: '#FFFFFF',
    navbar: '#FFFFFF',
  },
};
